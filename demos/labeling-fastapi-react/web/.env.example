# Copy this file to .env.local (gitignored) to override. The default
# below matches api/main.py's uvicorn command in ../README.md - only
# needed if you run the API on a different host/port.
VITE_API_URL=http://127.0.0.1:8000
