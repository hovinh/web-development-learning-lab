# Top-level Python dependencies for this repo.
# List only direct dependencies here; run `pip-compile` to generate the
# fully pinned, transitively-resolved requirements.txt.
#
# Example:
# fastapi

# read-write-data: ORM access to the SQLite database.
SQLAlchemy

# read-write-data: lower-boilerplate SQLite access, for comparison with
# the raw SQLAlchemy ORM version.
dataset

# read-write-data: MongoDB access, for comparison with the SQL versions.
pymongo
